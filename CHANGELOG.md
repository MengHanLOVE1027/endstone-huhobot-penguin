# 📋 Changelog

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/) 格式。

---

## [0.1.0-beta.2] - 2026-08-17

### 无

---

## [0.1.0-beta.1] - 2026-08-17

首个 Beta 版本：完成 QQ 开放平台官方机器人 ↔ BDS 之间的聊天 / 命令桥接，基本功能对齐 Java 版。

### ✨ 新增

- **QQ ↔ 游戏聊天桥接**
  - 游戏聊天以 `chat-format.start-with`（默认 `#`）开头时，转发到所有已配置群。
  - 群内 @ 机器人的消息（命令或全量转发）广播进游戏。
  - 双向消息格式均可通过 `chat-format.*` 配置。

- **QQ 官方网关接入（零依赖）**
  - 用标准库 `socket` + `ssl` 自实现 RFC6455 WebSocket 客户端，直连 QQ 开放平台正式环境。
  - 支持 access_token 获取、心跳保活、断线重连（Resume / 重新 Identify）。
  - REST 走标准库 `urllib.request`，无需任何第三方依赖。

- **22 个内置命令**（群内 @ 机器人触发，⭐ 需管理员）
  - 信息查询：`查信息`、`查在线`、`查白名单`、`查管理`、`在线服务器`、`motd`。
  - 消息转发：`发信息` / `发消息`。
  - 命令执行：`执行`、`执行命令` ⭐、`管理员执行` ⭐。
  - 管理员管理：`加管理`、`删管理`、`管理方式`、`认证`、`解除认证`。
  - 白名单：`添加白名单`、`删除白名单`、`查白名单`、`绑定白名单`、`解绑白名单`、`解除绑定`。
  - 其他：`全量` 转发开关 ⭐。
  - 每个命令可用 `commands.*` 单独开关。

- **配置系统**
  - 嵌套 JSON 配置（`config.json`），首次启动自动生成完整默认项。
  - 缺省补全 + `config-version` 版本迁移（旧键自动升级）。

- **自定义命令**
  - 配置驱动的 BDS 控制台命令，支持 `permission` 分级与 `{params}` / `{0}` / `{group}` / `{user}` 占位符。

- **白名单 / 管理员 / 认证**
  - 管理员判定三种方式：QQ 群主管理员 / 手动添加 / 双重。
  - QQ 与游戏名绑定（绑定白名单）与认证状态，落盘持久化。

- **Markdown 卡片**
  - `查在线`、`查白名单`、`motd` 支持 Markdown 卡片（含 MOTD 状态图），解析失败自动回退纯文本。

- **进服 / 退服通知**
  - 玩家进/退服时向群推送通知，模板可配置。

- **内容审核**
  - 内置敏感词 + `filter-regex` 正则 + OpenAI 兼容 AI 二审（可选）。

- **日志**
  - 控制台彩色输出（渐变彩虹正文）+ 按日文件日志。

- **控制台命令**
  - `/huhobot reload`：重载配置并重连机器人（无需重启服务器）。
  - `/huhobot info`：查看平台 / 版本 / 模式。

---